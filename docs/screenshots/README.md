Add frontend screenshots here so they render in the repository `README.md`.

Filenames expected by the root README (place your images with these exact names):

- `dashboard.png`         — Dashboard (API status, model/scaler status, supported symbols)
- `predict.png`           — Predict form (technical indicator inputs)
- `train.png`             — Training & pipeline operations UI
- `data_viewer.png`       — Stock Data Viewer (processed data table)

Recommended image settings:
- Format: PNG
- Max width: 1600px (keeps README readable)
- Compression: use lossless or medium-quality to keep file sizes reasonable

After adding images to `docs/screenshots/`, commit them:

```bash
git add docs/screenshots/dashboard.png docs/screenshots/predict.png docs/screenshots/train.png docs/screenshots/data_viewer.png
git commit -m "Add frontend screenshots for README"
git push
```

If you prefer, I can add these images for you — upload the four PNGs here or tell me their current paths and I'll commit them into `docs/screenshots/`.